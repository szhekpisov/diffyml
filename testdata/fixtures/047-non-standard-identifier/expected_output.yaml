Found two differences

scrape_configs.kubernetes-apiservers.scheme
  ± value change
    - http
    + https

scrape_configs.kubernetes-apiservers.relabel_configs.0.regex
  ± value change
    - default;kubernetes;http
    + default;kubernetes;https

