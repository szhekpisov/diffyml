Found four unchanged values

name
  = one map entry unchanged:
    name: app

image.repo
  = one map entry unchanged:
    repo: nginx

labels.app
  = one map entry unchanged:
    app: web

ports.0
  = one list entry unchanged:
    - 80

